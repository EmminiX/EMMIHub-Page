{
  "name": "emmihub-landing-page",
  "version": "1.0.0",
  "description": "EMMIHUB - A cognitive enhancement platform for human-AI collaboration",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=8000 --no-browser",
    "dev": "live-server --port=8000 --no-browser",
    "build": "npm run lint && npm run format",
    "lint": "eslint src/js/**/*.js",
    "format": "prettier --write \"src/**/*.{js,css,html}\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "emmihub",
    "ai",
    "cognitive-enhancement",
    "human-ai-collaboration"
  ],
  "author": "Emmi C.",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.57.0",
    "live-server": "^1.2.2",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "gsap": "^3.12.7"
  }
}
